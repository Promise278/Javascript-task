// 6 find the primes numbers

let primenum = [2, 4, 6, 8];

function primeNums(number, primenum) {
    if (primenum / 2) {
        console.log("these are prime numbers")
    } else {
        console.log("these are even")
    }
}

console.log(primeNums(2))